spring:
  datasource:
    url: "jdbc:mysql://${MYSQL_HOST}:3306/spring-boot-blueprint?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC"

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
