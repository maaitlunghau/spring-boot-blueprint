# Code Templates — spring-boot-blueprint

No controller, service, repository, entity, or DTO has been written in this project yet, so there's no real pattern to copy from. Don't paste in generic boilerplate here speculatively — once the first domain feature exists, replace this file with the actual style used, matching `architecture.md`.
